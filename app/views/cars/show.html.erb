
<h2><%= @car.model %></h2>
<h2><%= @car.make %></h2>

<% @car.transactions.last(5).each do |t| %>
  <%= t.amount %>
<% end %>

<%= simple_form_for [@car, @transaction] do |f| %>
  <%= f.label :amount %> <%= f.input :amount %>
  <%= f.submit %>
<% end %>
